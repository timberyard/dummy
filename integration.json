{	"name": "dummy",
	"variant": "c++",
	"tasks": {
		"build": {
			"source": "${input}/src",
			"install": {
				"enabled": "no"
			}
		},
		"test": {
			"binary": "${output}/build/googletest"
		}
	}
}